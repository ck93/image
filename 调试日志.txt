10.18晚
完成了基本的读取和保存图片功能，其中出现的问题是按照网上的代码写了之后图片不正确，后来发现原因是图片的宽和高是设定成picturebox的宽和高，实际上它只是显示的时候进行了缩放和拉伸，图片宽高应该还是原始宽高。
参考代码：http://www.cnblogs.com/yiyiruohan/archive/2010/08/24/1807533.html
10.27晚
打开非正方形图片后旋转范围很小，后查明是获取limit时没有注意到数组的长宽和图片的长宽相反。
非正方形图片会出现数组越界问题，经检查是判断的时候将site[0]和site[1]弄反
11.2晚
最近邻插值将int x = Convert.ToInt32(site[0])改为int x = (int)(site[0] + 0.5)，提高速度接近100%。